Just execute the program the it will connect to the database stored at jdbc:mySQL://localhost:3306/it_sss automatically.

Main class is the main program
DialogBox Classes provide User entry for the corresponding buttons 